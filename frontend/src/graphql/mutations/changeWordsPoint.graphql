mutation changeWordsPoint($input: [WordsPointListInput]) {
    changeWordsPoint(input: $input) {
        data {
            id
            attributes {
                ...Word
            }
        }
    }
}