mutation updateVerb($id: ID!, $data: VerbInput!) {
    updateVerb(id: $id, data: $data) {
        data {
            id
            attributes {
                ...Verb
            }
        }
    }
}